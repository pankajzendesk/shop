apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  PORT: "3000"
  NODE_ENV: "production"
  NEXT_TELEMETRY_DISABLED: "1"
  POSTGRES_DB: "shop_data"
  NEXT_PUBLIC_SHOP_NAME: "GadgetToyShop"
